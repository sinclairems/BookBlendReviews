<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>BookBlend</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Marcellus&display=swap" rel="stylesheet">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Fira+Sans&family=Marcellus&display=swap" rel="stylesheet">
</head>

<body>
    <header class="header">
      <nav class="nav">
        <a href="/">Home</a> 
        <a href="/book">View Books</a>
        <a href="/review">Write a Review</a>
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <a href="/profile">Profile</a> |
        <a href="/logout" id="logout">Logout</a>
        {{else}}
        <a href="/login">Login</a>
        {{/if}}
      </nav>
      <div>
        <form id="search-form" action="/api/search/results" method="post">
          <input class="input" type="text" id="search-input" name="search" placeholder="Search...">
          <button class="button" type="submit" id="search-submit">Search</button>
        </form>
        <div id="search-results"></div>
      </div>
    </header>
    <main class="main">
      <h1>Book Blend Reviews</h1>
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    <footer class="footer">
      <p>Book Blend Reviews</p>
    </footer>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
  <script src="/js/search.js"></script>
</body>

</html>